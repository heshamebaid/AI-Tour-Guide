# =============================================================================
# AI Tour Guide – ONE .env for the whole project (project root only)
# Copy to .env and set OPEN_ROUTER_API_KEY. All services read from this file.
# =============================================================================

# ---- OpenRouter (LLM: chatbot, pharos, place-details, translation) ----
# Use KEY=value with no spaces around = so Docker and scripts read it correctly.
OPEN_ROUTER_API_KEY="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"
OPEN_ROUTER_MODEL=liquid/lfm-2.5-1.2b-thinking:free
# LLM_MODEL=  # optional alias

# ---- SerpAPI (optional: image search in RAG) ----
# SERPAPI_API_KEY=your_serpapi_key_here

# ---- Embeddings & RAG (Agentic_RAG, Chatbot, Pharos) ----
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ---- Qdrant vector DB ----
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION_NAME=tour_guide_documents
# QDRANT_API_KEY=  # optional for Qdrant Cloud

# ---- Hybrid search & reranker ----
ENABLE_HYBRID_SEARCH=true
DENSE_WEIGHT=0.5
SPARSE_WEIGHT=0.5
ENABLE_RERANKER=true
RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2
RERANKER_INITIAL_K=20

# ---- Paths (optional; defaults used if unset) ----
# VECTORSTORE_PATH=./vectorstore
# DOCUMENTS_PATH=./data/raw
# PROCESSED_DATA_PATH=./data/processed

# ---- Talk to Pharos service ----
# PHAROS_HOST=0.0.0.0
# PHAROS_PORT=8050
# PHAROS_ALLOWED_ORIGINS=*
# PHAROS_SKIP_STARTUP=  # set 1 to skip RAG load on startup

# ---- Place Details service ----
# PLACE_DETAILS_ALLOWED_ORIGINS=*

# ---- Django (when run locally; Docker sets these) ----
# PHAROS_SERVICE_URL=http://localhost:8050
# PHAROS_PUBLIC_URL=http://localhost:8050
# PLACE_DETAILS_SERVICE_URL=http://localhost:8060
# TRANSLATION_SERVICE_URL=http://localhost:8000

# ---- Translation / hieroglyph pipeline (optional) ----
# DISABLE_RAG_REFINEMENT=  # 1/true to skip RAG story refinement
# FALLBACK_MODE=  # 1/true for fallback story when LLM fails
# NO_LLM_MODE=  # 1/true to disable LLM story
